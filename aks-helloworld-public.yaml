apiVersion: apps/v1
kind: Deployment
metadata:
  name: aks-helloworld-public  
spec:
  replicas: 1
  selector:
    matchLabels:
      app: aks-helloworld-public
  template:
    metadata:
      labels:
        app: aks-helloworld-public
    spec:
      containers:
      - name: aks-helloworld-public
        image: mcr.microsoft.com/azuredocs/aks-helloworld:v1
        ports:
        - containerPort: 80
        env:
        - name: TITLE
          value: "Welcome to PUBLIC Azure Kubernetes Service (AKS)"
---
apiVersion: v1
kind: Service
metadata:
  name: aks-helloworld-public  
spec:
  type: ClusterIP
  ports:
  - port: 80
  selector:
    app: aks-helloworld-public